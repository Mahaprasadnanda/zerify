{
  "name": "kyc",
  "version": "1.0.0",
  "description": "This repository is the starter monorepo for a web-based KYC system that uses Zero Knowledge Proofs (ZKPs) to validate user claims without transmitting sensitive identity data to the backend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "circomlib": "^2.0.5"
  }
}
