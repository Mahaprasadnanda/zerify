"use client";

/** Side effect: logs once that the app uses flexibleKyc artifacts only (see flexibleKycArtifacts.ts). */
import "@/utils/flexibleKycArtifacts";

export function ZkpArtifactLog() {
  return null;
}
