"use client";

/** Side effect: loads flexibleKyc artifact config (assert + dev-only safe log; see flexibleKycArtifacts.ts). */
import "@/utils/flexibleKycArtifacts";

export function ZkpArtifactLog() {
  return null;
}
